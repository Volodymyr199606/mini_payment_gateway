# Copy this file to .env and fill in your PostgreSQL credentials.
# .env is gitignored and will not be committed.

# PostgreSQL (required for db:create, db:migrate, etc.)
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=
