<!DOCTYPE html>
<html>
  <head>
    <title>Dashboard - Mini Payment Gateway</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "dashboard", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="dashboard">
    <%# Step 1: Layout backbone — top bar, content container, no JS %>
    <nav class="nav" aria-label="Main">
      <div class="nav-container">
        <div class="nav-logo">
          <%= link_to "Mini Payment Gateway", dashboard_root_path, class: "nav-logo-link" %>
        </div>
        <div class="nav-menu">
          <%= link_to "Transactions", dashboard_transactions_path, class: "nav-link" %>
          <%= link_to "Ledger", dashboard_ledger_index_path, class: "nav-link" %>
          <%= link_to "Account", dashboard_account_path, class: "nav-link" %>
          <span class="nav-merchant"><%= current_merchant.name %></span>
          <%= button_to "Sign Out", dashboard_sign_out_path, method: :delete, class: "nav-link nav-link-button", data: { turbo: false } %>
        </div>
      </div>
    </nav>

    <main class="main" role="main">
      <% if notice %>
        <div class="alert alert-success"><%= notice %></div>
      <% end %>
      <% if alert %>
        <div class="alert alert-error"><%= alert %></div>
      <% end %>

      <div class="content-container">
        <%= yield %>
      </div>
    </main>
  </body>
</html>
